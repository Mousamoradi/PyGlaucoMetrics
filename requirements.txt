[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "PyGlaucoMetrics"
version = "0.1.0"
description = "Visual field analysis for glaucoma without R dependencies"
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.9"
authors = [{name = "Your Name", email = "you@email.com"}]
dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "matplotlib>=3.7",
    "scipy>=1.10",
    "PyQt5>=5.15",
    "Pillow>=9.0",
    "seaborn>=0.12",
    "pingouin>=0.5",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"PyGlaucoMetrics" = ["data/*.csv"]

[project.urls]
Homepage = "https://github.com/YourGithub/PyGlaucoMetrics"